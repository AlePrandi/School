/*
author: Prandi Alessandro
date:
es.
testo
*/

#include <stdio.h>
#include <stdlib.h>
#define GMESE 30

void GiorniLib(int gLib[],){
for(g = 1; g <= GMESE; g++) {
    if(NonOrdDisg(g1,n1,g)== -1 && NonOrdDisg(g2,n2,g)== -1) {
        gLib[j] == g;
        j++;
    }
}
}

void ImpMax(){
int max = cercaMax(imp1, n1);
stampaGioMax(imp1,g1, max, n1);
}

int main() {

    int giorni1[DIM] = {12, 4, 15, 2};
    int impegni1[DIM] = {1, 7, 6, 7};

    int giorni2[DIM] = {12, 4, 15, 2};
    int impegni2[DIM] = {1, 7, 6, 7};

    return 0;
}
